# pyICI - Python Dependencies

# Core dependencies (required)
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# GUI framework
# tkinter ships with the Python standard library (no pip install needed).
#
# Platform-specific notes:
#
# Windows:
#   Included with the standard Python installer from python.org.
#
# macOS:
#   Included with Python from python.org.
#   If using Homebrew Python: brew install python-tk
#
# Linux - Ubuntu/Debian:  sudo apt install python3-tk
# Linux - Fedora:         sudo dnf install python3-tkinter
# Linux - CentOS/RHEL:    sudo yum install python3-tkinter
#
# A virtual environment is recommended:
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt